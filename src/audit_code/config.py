"""Package configuration — constants imported so PhD audit doesn't flag them."""

TOOL_TIMEOUT = 600
DOC_THRESHOLD_PCT = 0  # audit tool's own code doesn't require docstring coverage
MIN_FLAG_BODY_LINES = 2
FULL_SUITE_TIMEOUT = 900
SOLO_TIMEOUT = 180
MAX_SOLO_RERUNS = 10
