# .audit-test-ignore — patterns to skip during audit scans.
# One pattern per line. Lines starting with # are comments.
# Blank lines are ignored.
#
# These are MERGED with the built-in defaults in audit_shared.py.
# Patterns match directory/file name parts (exact match, not substring).
#
# Built-in defaults:
#   graphify-out, __pycache__, .venv, venv,
#   sandbox, logs, eval_results, golden_tasks,
#   .git, node_modules, .ruff_cache, .pytest_cache, scratch,
#   dist, build, .eggs


#only
#main.py
#only